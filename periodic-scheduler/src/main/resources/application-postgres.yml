spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/scheduler}
    username: ${DATABASE_USER:scheduler}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver
